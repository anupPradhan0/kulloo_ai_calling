# Baked into `pnpm build` / Docker. Browser talks to kulloocall only; nginx proxies /api → API.
# Do not point the SPA at app.anuppradhan.in until that host has a valid TLS cert for that name.
VITE_USE_RELATIVE_API=true
