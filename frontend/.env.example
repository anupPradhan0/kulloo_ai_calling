# =============================================================================
# Kulloo frontend — copy to .env.local for local overrides (not committed)
# =============================================================================

# -----------------------------------------------------------------------------
# Build-time (Vite) — production / Nixpacks build
# -----------------------------------------------------------------------------
# Browser calls this host for /api/... No trailing slash.
#
# VITE_API_BASE_URL=https://kulloocall.anuppradhan.in

# -----------------------------------------------------------------------------
# Nixpacks runtime (optional — shown in startup banner logs)
# -----------------------------------------------------------------------------
# PUBLIC_URL=https://app.anuppradhan.in
