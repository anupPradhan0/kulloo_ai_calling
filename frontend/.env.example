# Default API base in the UI is https://kulloocall.anuppradhan.in (see frontend/src/api/constants.ts).
# Override for a local or other backend:
# VITE_API_BASE_URL=http://127.0.0.1:5000
#
# --- Production (Docker + nginx): optional; baked into some Docker builds ---
# VITE_USE_RELATIVE_API=true
